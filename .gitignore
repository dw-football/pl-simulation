.Rproj.user
.Rhistory
.RData
.Ruserdata
.obsidian/

# --- Generated simulation artifacts ---
# Regenerated by the pipeline (get_pl_data.R / simulate.R); the Shiny app builds
# them in-memory, so a fresh clone runs without them. Untracked to keep the tree
# clean so the launcher's `git pull --ff-only` never conflicts with run output.
data/sim_data.Rdata
data/league_table.csv
data/sorted_league_table.csv
data/rem_matches.csv
Rplots.pdf
Premier_League_Simulation_files/

# Maccabi 2023 Excel prototype (pre-R model) — archived to ~/My Drive/Soccer/Maccabi/Data/
data/maccabi/
